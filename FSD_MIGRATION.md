# Миграция к Feature-Sliced Design - Завершение

## ✅ Выполнено автоматически

1. **Создан слой `entities`**
   - `src/entities/user/` - сущность пользователя с API
   - Структура: `api/`, `model/`, `index.ts`

2. **Реорганизованы `features` с сегментами**
   - `src/features/auth/` - с `api/` и `model/`
   - `src/features/invite/` - с `api/`
   - `src/features/showcase/` - с `api/` и `model/`
   - Все features теперь имеют правильную структуру Public API через `index.ts`

3. **Реорганизованы `pages` с сегментом `ui/`**
   - `src/pages/home/ui/`
   - `src/pages/bonuses/ui/`
   - `src/pages/profile/ui/`
   - `src/pages/favorites/ui/`
   - `src/pages/invite/ui/`
   - Все pages экспортируют компоненты через `index.ts`

4. **Обновлены импорты**
   - Все импорты из `@/features/user` заменены на `@/entities/user`
   - Импорты из features обновлены для использования Public API
   - Импорты из `@assets` заменены на `@shared/assets`

5. **Обновлена конфигурация**
   - `vite.config.ts` - добавлены алиасы для `@entities` и `@features`
   - Алиас `@assets` теперь указывает на `src/shared/assets`

## 🔧 Требуется выполнить вручную

### 1. Переместить assets в shared

Необходимо переместить содержимое папки `src/assets/` в `src/shared/assets/`:

```bash
# PowerShell команды (выполнить в корне проекта)

# Переместить папку animations
Move-Item -Path "src\assets\anomation" -Destination "src\shared\assets\animations"

# Переместить иконки (объединить с существующей папкой icons)
Get-ChildItem "src\assets\icons\*.svg" | ForEach-Object {
    Move-Item -Path $_.FullName -Destination "src\shared\assets\icons\"
}

# Переместить изображения
Move-Item -Path "src\assets\images" -Destination "src\shared\assets\images"

# Удалить старую папку assets после проверки
Remove-Item -Path "src\assets" -Recurse
```

**Или через проводник Windows:**
1. Скопируйте папку `src/assets/anomation` в `src/shared/assets/` и переименуйте в `animations`
2. Скопируйте файлы из `src/assets/icons/` в `src/shared/assets/icons/`
3. Скопируйте папку `src/assets/images` в `src/shared/assets/`
4. Удалите папку `src/assets`

### 2. Проверить работу приложения

После перемещения assets:

```bash
npm run dev
```

Проверьте:
- Загружаются ли все изображения
- Работают ли все страницы
- Нет ли ошибок в консоли

### 3. (Опционально) Дополнительная реорганизация widgets

Некоторые widgets можно дополнительно реорганизовать с сегментом `ui/`, как это сделано для `header` и `loginModal`. Примеры:

```
src/widgets/sidebar/
  ├── ui/
  │   ├── Sidebar.tsx
  │   ├── Sidebar.module.scss
  │   ├── CategorySwitcherWithSearch/
  │   ├── MenuSection/
  │   └── SidebarFooter/
  └── index.ts
```

Это не критично, так как основная структура widgets уже хорошая.

## 📋 Структура проекта после миграции

```
src/
├── app/                      # Слой приложения
│   ├── layouts/
│   ├── store/
│   └── App.tsx
├── pages/                    # Страницы
│   ├── home/
│   │   ├── ui/
│   │   │   ├── HomePage.tsx
│   │   │   └── HomePage.module.scss
│   │   └── index.ts
│   ├── bonuses/
│   ├── profile/
│   ├── favorites/
│   └── invite/
├── widgets/                  # Виджеты (композитные блоки UI)
│   ├── header/
│   ├── footer/
│   ├── sidebar/
│   └── ...
├── features/                 # Бизнес-фичи (действия пользователя)
│   ├── auth/
│   │   ├── api/
│   │   ├── model/
│   │   └── index.ts
│   ├── invite/
│   └── showcase/
├── entities/                 # Бизнес-сущности
│   └── user/
│       ├── api/
│       ├── model/
│       └── index.ts
└── shared/                   # Общий код
    ├── api/
    ├── assets/               # 👈 Все ассеты здесь
    │   ├── fonts/
    │   ├── icons/
    │   ├── images/
    │   └── animations/
    ├── config/
    ├── lib/
    ├── model/
    ├── styles/
    └── ui/
```

## 🎯 Принципы FSD

### Слои (Layers)
- **app** - инициализация приложения, провайдеры, роутинг
- **pages** - страницы приложения
- **widgets** - большие композитные блоки UI
- **features** - действия пользователя (auth, invite)
- **entities** - бизнес-сущности (user)
- **shared** - переиспользуемый код

### Сегменты (Segments)
Внутри слайсов (features, entities, widgets):
- **api** - запросы к API
- **model** - бизнес-логика, хуки, типы
- **ui** - UI компоненты
- **lib** - вспомогательные функции
- **config** - конфигурация и фича-флаги

### Public API
Каждый слайс экспортирует только публичное API через `index.ts`:
```typescript
// ✅ Хорошо
export { useAuthFlow } from './model/useAuthFlow';
export { authenticateTelegramWebApp } from './api/api';

// ❌ Плохо - не экспортируем внутренние детали
export * from './api/api';
export * from './model/useAuthFlow';
```

### Правило импортов
Слой может импортировать только из слоёв ниже:
- `pages` → `widgets`, `features`, `entities`, `shared`
- `widgets` → `features`, `entities`, `shared`
- `features` → `entities`, `shared`
- `entities` → `shared`
- `shared` → ничего

## 📚 Дополнительные ресурсы

- [Feature-Sliced Design документация](https://feature-sliced.design/)
- [Примеры проектов](https://github.com/feature-sliced/examples)

## ✨ Результат

После завершения миграции проект будет:
- ✅ Соответствовать архитектуре FSD
- ✅ Иметь чёткую структуру и разделение ответственности
- ✅ Легче масштабироваться
- ✅ Проще для понимания новыми разработчиками

