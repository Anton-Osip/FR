# Скрипты для разработки

## check-fsd.js - Проверка структуры Feature-Sliced Design

Валидатор архитектуры FSD, который проверяет соблюдение правил импортов между слоями.

### Использование

```bash
# Проверить всю структуру проекта
node scripts/check-fsd.js

# Проверить конкретную директорию
node scripts/check-fsd.js src/pages

# Проверить конкретный файл
node scripts/check-fsd.js src/pages/home/HomePage.tsx
```

### Что проверяет

1. **Правила импортов между слоями:**
   - `app` → может импортировать из всех слоев ниже
   - `pages` → может импортировать из: `widgets`, `features`, `entities`, `shared`
   - `widgets` → может импортировать из: `features`, `entities`, `shared`
   - `features` → может импортировать из: `entities`, `shared`
   - `entities` → может импортировать из: `shared`
   - `shared` → не может импортировать из других слоев

2. **Использование public API:**
   - Предупреждает, если используется прямой импорт из файла вместо `index.ts`

3. **Алиасы:**
   - Проверяет корректность использования алиасов (`@app`, `@pages`, `@widgets`, `@shared`)

### Пример вывода

```
🔍 Проверка 150 файлов...

📊 Результаты проверки:

❌ Найдено ошибок: 2

src/pages/home/HomePage.tsx:5
  ❌ Нарушение FSD: слой "pages" не может импортировать из слоя "pages"
  Импорт: @pages/profile

⚠️  Найдено предупреждений: 1

src/widgets/header/Header.tsx:10
  ⚠️  Рекомендуется использовать public API через index.ts вместо прямого импорта
  Импорт: @shared/ui/button/Button.tsx
```

---

## clean-imports.js - Очистка неиспользуемых импортов

Инструмент для поиска и удаления неиспользуемых импортов в TypeScript/TSX файлах.

### Использование

```bash
# Предварительный просмотр (dry-run) - не изменяет файлы
node scripts/clean-imports.js

# Проверить конкретную директорию
node scripts/clean-imports.js src/features/auth

# Проверить конкретный файл
node scripts/clean-imports.js src/pages/home/HomePage.tsx

# Удалить неиспользуемые импорты (применить изменения)
node scripts/clean-imports.js --write

# Удалить импорты в конкретной директории
node scripts/clean-imports.js src/features/auth --write
```

### Что делает

1. **Анализирует использование импортов:**
   - Проверяет использование всех именованных импортов
   - Проверяет использование default импортов
   - Проверяет использование namespace импортов
   - Различает type-only импорты

2. **Безопасное удаление:**
   - Сохраняет форматирование кода
   - Удаляет только неиспользуемые импорты
   - Поддерживает `import type` и inline type imports

### Пример вывода

**Режим dry-run (без --write):**
```
ℹ️  Режим предварительного просмотра (dry-run)
   Используйте --write для применения изменений

🔍 Анализ 150 файлов...

📊 Найдено неиспользуемых импортов: 5

💡 Для удаления импортов запустите скрипт с флагом --write

⚠️  Найдено: src/pages/home/HomePage.tsx (2 импорта)
⚠️  Найдено: src/widgets/header/Header.tsx (3 импорта)
```

**Режим с --write:**
```
🔍 Анализ 150 файлов...

📊 Найдено неиспользуемых импортов: 5

✅ Удалено: src/pages/home/HomePage.tsx (2 импорта)
✅ Удалено: src/widgets/header/Header.tsx (3 импорта)

✅ Удалено 5 неиспользуемых импортов из 2 файлов
```

### Важные замечания

- **Всегда используйте dry-run сначала** для проверки перед применением изменений
- Скрипт не удаляет импорты, которые используются только как типы (type-only imports)
- Скрипт сохраняет форматирование, но рекомендуется запустить Prettier после применения изменений

---

## create-component.js - Генератор компонентов

Скрипт для автоматического создания структуры React компонента.

### Использование

```bash
node scripts/create-component.js <ComponentName> <targetPath>
```

### Примеры

```bash
# Создать компонент Button в shared/ui
node scripts/create-component.js Button src/shared/ui

# Создать компонент UserCard в widgets
node scripts/create-component.js slotWindow src/widgets
```

### Создаваемая структура

```
componentName/
  ComponentName.tsx      # Компонент с интерфейсом пропсов
  ComponentName.module.scss  # Файл стилей с @use для переменных
  index.ts               # Реэкспорт компонента
```

---

## Рекомендации

1. **Перед коммитом:**
   ```bash
   node scripts/check-fsd.js
   node scripts/clean-imports.js --write
   ```

2. **При создании новой фичи:**
   - Используйте `create-component.js` для быстрого создания компонентов
   - Проверьте структуру с помощью `check-fsd.js`

3. **Регулярная очистка:**
   - Запускайте `clean-imports.js` периодически для поддержания чистоты кода

